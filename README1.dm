# Memory Match — Игра на память

Интерактивная игра на развитие кратковременной памяти, написанная на **Python** с использованием библиотеки **Pygame**.



## 🎮 Особенности

- Три уровня сложности: **2×2**, **4×4**, **6×6**
- Автоматическое перемешивание карточек
- Три секунды на запоминание расположения
- Подсветка найденных пар (зелёный) и ошибок (красный)
- Ведение рекордов по каждому уровню (сохраняются в `records.json`)
- Поддержка горячих клавиш:
  - `R` — начать новую игру (после победы)
  - `ESC` или `Q` — вернуться к выбору уровня

## ▶️ Как запустить

1. Убедитесь, что у вас установлен **Python 3.8+**
2. Установите зависимости:

python memory_game.py
pip install pygame
📁 Структура проекта
.
├── memory_game.py      # Основной код игры
├── records.json        # Файл рекордов (создаётся автоматически)
├── README.md           # Этот файл
├── .gitignore          # Исключения для Git

└── Dockerfile          # (Опционально) для сборки Docker-образа
